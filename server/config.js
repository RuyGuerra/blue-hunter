const config = {
    app: {
      port: 3000
    },
    db: {
      config: {
        user: 'sa',
        password: 'litiummuon',
        server: 'localhost',
        database: 'bluehunter'
      }
    }
   };

   module.exports = config;