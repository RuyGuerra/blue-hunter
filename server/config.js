const config = {
    app: {
      port: 3000
    },
    db: {
      config: {
        user: '*****',
        password: '******',
        server: '*****',
        database: 'bluehunter'
      }
    }
   };

   module.exports = config;